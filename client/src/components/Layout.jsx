import {Link,NavLink} from 'react-router-dom';import {useAuth} from '../context/AuthContext';import {useCart} from '../context/CartContext';
export default function Layout({children}){const {user,logout}=useAuth();const {count}=useCart();return <><header><Link className="brand" to="/">NOVA<span>STORE</span></Link><nav><NavLink to="/">Shop</NavLink>{user&&<NavLink to="/orders">Orders</NavLink>}{user?.role==='admin'&&<NavLink to="/admin">Admin</NavLink>}</nav><div className="nav-actions">{user?<><span className="hello">Hi, {user.name.split(' ')[0]}</span><button className="link-btn" onClick={logout}>Logout</button></>:<Link to="/login">Login</Link>}<Link className="cart" to="/cart">Bag ({count})</Link></div></header><main>{children}</main><footer><div>NOVA STORE</div><span>Modern essentials. Simple shopping.</span></footer></>}
