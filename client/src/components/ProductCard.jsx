import {Link} from 'react-router-dom';import {useCart} from '../context/CartContext';export default function ProductCard({p}){const {add}=useCart();return <article className="card"><Link to={`/products/${p._id}`}><img src={p.image}/></Link><div className="card-body"><div className="muted">{p.category}</div><h3>{p.name}</h3><div className="row"><b>₹{p.price.toLocaleString('en-IN')}</b><button disabled={!p.stock} onClick={()=>add(p)}>Add</button></div>{p.stock<5&&p.stock>0&&<small>Only {p.stock} left</small>}</div></article>}
