import {useState} from 'react';import {Link,useLocation,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';export default function Auth({mode='login'}){const isLogin=mode==='login',loc=useLocation(),nav=useNavigate(),{login,register}=useAuth();const [f,setF]=useState({name:'',email:'',password:''}),[err,setErr]=useState('');const submit=async e=>{e.preventDefault();try{isLogin?await login(f):await register(f);nav(loc.state?.from||'/')}catch(x){setErr(x.response?.data?.message||'Something went wrong')}};return <section className="auth"><form onSubmit={submit}><p className="eyebrow">NOVA STORE</p><h1>{isLogin?'Welcome back':'Create account'}</h1>{!isLogin&&<input placeholder="Full name" required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/>}<input type="email" placeholder="Email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input type="password" placeholder="Password (6+ characters)" minLength="6" required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/>{err&&<div className="alert">{err}</div>}<button className="primary wide">{isLogin?'Login':'Register'}</button><p>{isLogin?<>New here? <Link to="/register">Create an account</Link></>:<>Already have an account? <Link to="/login">Login</Link></>}</p></form></section>}
