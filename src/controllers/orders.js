import Order from '../models/Order.js'; import Product from '../models/Product.js';
export const create=async(req,res)=>{const {items,shipping}=req.body;if(!items?.length)return res.status(400).json({message:'Cart is empty'});let total=0;const snapshot=[];for(const item of items){const p=await Product.findById(item.product);if(!p)return res.status(404).json({message:'Product not found'});const q=Number(item.quantity);if(!Number.isInteger(q)||q<1)return res.status(400).json({message:'Invalid quantity'});if(q>p.stock)return res.status(400).json({message:`Only ${p.stock} units available for ${p.name}`});total+=p.price*q;snapshot.push({product:p._id,name:p.name,image:p.image,price:p.price,quantity:q});}for(const i of snapshot)await Product.findByIdAndUpdate(i.product,{$inc:{stock:-i.quantity}});const order=await Order.create({user:req.user._id,items:snapshot,shipping,total});res.status(201).json(order)};
export const myOrders=async(req,res)=>res.json(await Order.find({user:req.user._id}).sort({createdAt:-1}));
export const getOne=async(req,res)=>{const o=await Order.findById(req.params.id).populate('user','name email');if(!o)return res.status(404).json({message:'Order not found'});if(req.user.role!=='admin'&&String(o.user._id)!==String(req.user._id))return res.status(403).json({message:'Not allowed'});res.json(o)};
export const all=async(req,res)=>res.json(await Order.find().populate('user','name email').sort({createdAt:-1}));
export const updateStatus=async(req,res)=>{const allowed=['Placed','Processing','Shipped','Delivered','Cancelled'];if(!allowed.includes(req.body.status))return res.status(400).json({message:'Invalid status'});const o=await Order.findByIdAndUpdate(req.params.id,{status:req.body.status},{new:true});if(!o)return res.status(404).json({message:'Order not found'});res.json(o)};
