import {Router} from 'express'; import {create,myOrders,getOne,all,updateStatus} from '../controllers/orders.js'; import {protect,adminOnly} from '../middleware/auth.js'; const r=Router();r.post('/',protect,create);r.get('/mine',protect,myOrders);r.get('/admin/all',protect,adminOnly,all);r.get('/:id',protect,getOne);r.patch('/:id/status',protect,adminOnly,updateStatus);export default r;
